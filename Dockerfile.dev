FROM maven:3.9.9-eclipse-temurin-21

WORKDIR /usr/src/app

COPY pom.xml .
# lombok.config avec le pom : sans lui, Lombok ne recopie pas les @Qualifier sur les constructeurs générés.
COPY lombok.config .
RUN mvn dependency:go-offline -B

COPY src ./src

CMD ["mvn", "spring-boot:run"]
